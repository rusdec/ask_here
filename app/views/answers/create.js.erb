<% if @answer.persisted? %>
  if (!$('table.answers tbody').length) {
    $('table.answers').append('<tbody></tbody>')
  }
  $('<%= j render @answer %>').appendTo('table.answers tbody')
<% else %>
  $('<%= render 'shared/errors', resource: @answer %>').appendTo('#errors')
<% end %>

